import styled from 'styled-components';

import BASE_SIZE from '../../constants/baseSize';
import Wrapper from '../Wrapper';

export const Container = styled(Wrapper)`
  height: ${BASE_SIZE * 10}px;
`;
